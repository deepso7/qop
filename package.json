{
  "name": "qop",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "check": "ultracite check && pnpm check:packages && pnpm typecheck",
    "check:contracts": "forge fmt --root contracts --check && forge lint --root contracts && forge test --root contracts --force",
    "check:packages": "pnpm --filter @qop/identity --filter @qop/api --filter mobile run test",
    "fix": "ultracite fix",
    "typecheck": "pnpm --filter @qop/identity --filter @qop/api --filter mobile run typecheck"
  },
  "devDependencies": {
    "@effect/language-service": "0.87.2",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "typescript": "catalog:",
    "ultracite": "7.11.0"
  },
  "engines": {
    "node": "24.20.0"
  },
  "packageManager": "pnpm@12.3.4"
}
