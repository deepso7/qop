diff --git a/dist/sdk.d.ts b/dist/sdk.d.ts
index 62f1292814aea45c32571f456a639beda7899c94..8476c047fe9666bcc78154d132120579a2b01671 100644
--- a/dist/sdk.d.ts
+++ b/dist/sdk.d.ts
@@ -44,7 +44,7 @@ export declare class Stream {
     /** @internal */
     remoteWriteClosed(): void;
     /** @internal */
-    terminal(error?: unknown): void;
+    terminal(error?: unknown, graceful?: boolean): void;
 }
 /** Events emitted by a negotiated {@link Stream}. */
 export interface StreamEventMap {
diff --git a/dist/sdk.js b/dist/sdk.js
index f32d00d1490c39989944956703fea9f50235e4be..479701e7caec5e2cf11e4e6adc4ae7ab2b9cc579 100644
--- a/dist/sdk.js
+++ b/dist/sdk.js
@@ -111,7 +111,7 @@ export class Stream {
             return Promise.reject(new Error('Cannot call read() after subscribing to "data"'));
         }
         this.#mode = "pull";
-        if (this.#closed) {
+        if (this.#closed && this.#terminalError !== undefined) {
             return Promise.reject(this.#terminalError);
         }
         const buffered = this.#shift();
@@ -216,16 +216,25 @@ export class Stream {
         }
     }
     /** @internal */
-    terminal(error = new ClosedError()) {
+    terminal(error = new ClosedError(), graceful = false) {
         if (this.#closed) {
             return;
         }
         this.#closed = true;
-        this.#terminalError = error;
-        this.#fifo.length = 0;
-        this.#fifoBytes = 0;
+        // A clean FIN can be followed by StreamClosed in the same native batch.
+        // Preserve unread bytes and EOF while still rejecting resets and failures.
+        const eof = graceful && this.#remoteWriteClosed;
+        this.#terminalError = eof ? undefined : error;
+        if (!eof) {
+          this.#fifo.length = 0;
+          this.#fifoBytes = 0;
+        }
         for (const read of this.#reads.splice(0)) {
+          if (eof) {
+            read.resolve(this.#shift());
+          } else {
             read.reject(error);
+          }
         }
         this.#emit("closed");
         this.#listeners.clear();
@@ -998,7 +1007,7 @@ export class Minip2pBase {
                 clearPendingOpen(pending);
                 pending.reject(new StreamClosedError());
             }
-            stream?.terminal(new StreamClosedError("The stream closed"));
+            stream?.terminal(new StreamClosedError("The stream closed"), true);
         }
         else if (event.tag === P2pEvent_Tags.StreamData) {
             stream?.receive(event.inner.data);
diff --git a/src/sdk.ts b/src/sdk.ts
index fde561212067ef9c18fe23276edaa3ef8d7f5d00..8a548d94391a85c7b65970d489f102ac9abe93a2 100644
--- a/src/sdk.ts
+++ b/src/sdk.ts
@@ -238,7 +238,7 @@ export class Stream {
       );
     }
     this.#mode = "pull";
-    if (this.#closed) {
+    if (this.#closed && this.#terminalError !== undefined) {
       return Promise.reject(this.#terminalError);
     }
     const buffered = this.#shift();
@@ -353,16 +353,25 @@ export class Stream {
   }
 
   /** @internal */
-  terminal(error: unknown = new ClosedError()): void {
+  terminal(error: unknown = new ClosedError(), graceful = false): void {
     if (this.#closed) {
       return;
     }
     this.#closed = true;
-    this.#terminalError = error;
-    this.#fifo.length = 0;
-    this.#fifoBytes = 0;
+    // A clean FIN can be followed by StreamClosed in the same native batch.
+    // Preserve unread bytes and EOF while still rejecting resets and failures.
+    const eof = graceful && this.#remoteWriteClosed;
+    this.#terminalError = eof ? undefined : error;
+    if (!eof) {
+      this.#fifo.length = 0;
+      this.#fifoBytes = 0;
+    }
     for (const read of this.#reads.splice(0)) {
-      read.reject(error);
+      if (eof) {
+        read.resolve(this.#shift());
+      } else {
+        read.reject(error);
+      }
     }
     this.#emit("closed");
     this.#listeners.clear();
@@ -1313,7 +1322,7 @@ export class Minip2pBase {
         clearPendingOpen(pending);
         pending.reject(new StreamClosedError());
       }
-      stream?.terminal(new StreamClosedError("The stream closed"));
+      stream?.terminal(new StreamClosedError("The stream closed"), true);
     } else if (event.tag === P2pEvent_Tags.StreamData) {
       stream?.receive(event.inner.data);
     } else {
