// API-owned tables will be added here as enrollment and session routes land.
// Onchain registry state is read on demand and is not projected into Postgres.
export const apiSchemaVersion = 1 as const;
