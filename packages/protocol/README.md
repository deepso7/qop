# @qop/protocol

Shared pairing codecs, registry reads, and live-session authorization used by the phone and CLI.

This package has no React Native, Expo, Node filesystem, or HTTP clients. Platform adapters live in `mobile/` and `cli/`.
