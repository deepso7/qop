{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "generate": "drizzle-kit generate:sqlite --out ./src/migrations --breakpoints --schema=./src/schema.ts",
    "migrate": "tsx src/migrate",
    "studio": "drizzle-kit studio --port 3333",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@libsql/client": "0.4.0-pre.5",
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.29.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.6",
    "tsconfig": "workspace:*",
    "tsx": "^4.6.2",
    "typescript": "^5.3.3"
  }
}
